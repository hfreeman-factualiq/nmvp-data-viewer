[project]
name = "nmvp-data-viewer"
version = "0.1.0"
description = "NMVP SharePoint discovery analytics (precomputed Streamlit dashboard)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.28",
    "pandas>=2.0",
    "plotly>=5.18",
    "pyarrow>=14.0",
]

[tool.uv]
package = false
